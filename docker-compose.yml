version: "3.9"

services:
  video-ai:
    build: .
    container_name: video-ai
    ports:
      - "8000:8000"
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [ gpu ]
    volumes:
      - ./data:/tmp
